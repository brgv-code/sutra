/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ['www.pexels.com', 'images.unsplash.com', 'images.pexels.com'],
	},
}

module.exports = nextConfig
